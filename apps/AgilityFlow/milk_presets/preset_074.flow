{
  "name": "Flow Preset 74 (Grid)",
  "shader": "float2 g = frac(uv * 18.2 + t * 0.2); float grid = smoothstep(0.1, 0.0, min(g.x, g.y)) * (m + 0.1); float4 p = ptx.Sample(s1, uv * z); float3 col = float3(0.60, 0.40, 0.80) * grid * (m * 2.0 + 0.2); return saturate(p * 0.9 + float4(col, 1.0));"
}